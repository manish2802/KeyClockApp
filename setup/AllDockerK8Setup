#create docker image
docker network create keyclock-network

docker run -d \
  --name keycloak \
  -p 8080:8080 \         
  -e KC_BOOTSTRAP_ADMIN_USERNAME=admin \
  -e KC_BOOTSTRAP_ADMIN_PASSWORD=admin \
  -e KC_PROXY_HEADERS=xforwarded \
  -e KC_HOSTNAME="https://3bf49389da4d-10-244-4-7-8080.spca.r.killercoda.com/" \
  -e JAVA_OPTS_APPEND="-Xms256m -Xmx512m" \
  quay.io/keycloak/keycloak:26.3.3 \
  start-dev
  
    docker run -d \
  --name keyclock-app \
  --network  keyclock-network \
  -p 8081:8081 \
  manish28/keyclock_app:latest